// Package profile provides named scan profiles for portwatch.
// Each profile stores a set of hosts, ports, and protocol that can be
// referenced by name when running scans, avoiding repetitive CLI flags.
package profile
